<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.falldowndetectionserver.mapper.UserMapper">
    <insert id="insert">
        INSERT INTO TBL_USER (CAMERA_ID, USER_NAME, USER_PASSWORD, USER_PHONE, USER_ADDRESS)
        VALUES (#{cameraId}, #{userName}, #{userPassword}, #{userPhone}, #{userAddress});
    </insert>
</mapper>